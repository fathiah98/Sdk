This directory contains 'C' source codes, MS Visual Studio 2013 project files and the input files for a 3 x 3 Sudoku solver.
In order to use the application, ensure the input is keyed into the file "input.txt", and it should be in the same directory 
as the exe file. Run the exe file and the results should be displayed on a PC command console.